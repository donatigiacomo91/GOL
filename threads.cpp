#include <iostream>
#include <thread>
#include <vector>
#include "board.h"

int main(int argc, char* argv[]) {

    auto nw = atoi(argv[1]);
    auto rows = atoi(argv[2]);
    auto cols = atoi(argv[3]);

    return 0;
}
